# Docker imaji icin CPU-uyumlu bagimlilik listesi.
# requirements.txt'ten farki: GPU'ya ozel torch/onnxruntime-gpu/nvidia-* paketleri
# YOK -- bunlar hem cok buyuk (torch+cuda ~2.6GB) hem de GPU'suz makinelerde/paylasilan
# Docker imajlarinda gereksiz. Kod GPU'yu otomatik algiliyor (engines/base.py:gpu_available()),
# bu yuzden CPU'da calismasi icin hicbir ayar degistirmeye gerek yok.
opencv-contrib-python==4.10.0.84
numpy
pandas
rapidfuzz
rapidocr-onnxruntime
rapidocr==3.9.1
easyocr
pytesseract
requests
flask
